This folder is intended to hold PWA icons.

Replace the placeholder icons with your own PNGs if desired:
 - icon-192.png (192x192)
 - icon-512.png (512x512)

If you prefer, create real PNGs and put them here; the manifest currently uses inline data icons so the PWA will work without separate files, but providing proper PNGs is recommended for a real installable experience.
